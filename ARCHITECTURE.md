# 公開辯論資訊網：AI 與維護者瀏覽文件

這份文件是本專案的首要導覽。後續 AI 或維護者在修改網站前，應先讀本文件，再依任務查看對應檔案。網站目前部署於 <https://macrokernel3000.github.io/opendebate/>。

## 1. 專案定位

本站是純前端、免後端的台灣高中辯論公開資料庫。主要功能是查詢高中與高中職辯論賽事、逐場賽果、學校／隊伍戰績、勝場、公開榮譽、選手與辯題。資料並非完整母體，所有文字應維持「目前已收錄」「公開資料」的邊界。

## 2. 執行架構

網站可直接開啟 `index.html`，也可由 GitHub Pages 提供服務。載入順序不可任意交換：

1. `data/public-data.js`：宣告 `window.DEBATE_PUBLIC_DATA`。
2. `js/site-config.js`：宣告表單等站點設定。
3. `js/core.js`：資料正規化、查找與共用工具。
4. `js/interactions.js`：頁面切換、點擊、搜尋等互動綁定。
5. `js/personal-records.js`：個人成績建檔、瀏覽器暫存與 CSV 匯入／匯出。
6. `app.js`：建立賽事摘要並渲染所有畫面。

`index.html` 是單頁入口；`#home`、`#overview`、`#search`、`#archive`、`#reports` 是五個畫面狀態，不是五份獨立 HTML。主要導覽順序固定為「首頁、總覽、搜尋、建檔、回報」。舊連結 `#events` 仍會相容導向總覽的賽事分頁。

## 3. 資料流

主要人工編輯來源是 `data/public-data*.csv` 與 `data/entity-registry.csv`。正式慣例是一個賽事一個 CSV；舊 Excel 已移至 `data/backups`，只供復原。執行 `更新網站資料.command` 後，`tools/build_data.py` 會：

1. 掃描並合併所有 `public-data*.csv`。
2. 對完全相同的資料去重，保留歷史來源。
3. 同步單位名冊與別名。
4. 產生網站唯一讀取的 `data/public-data.js`。
5. 產生 `data/update-report.txt`，記錄時間、來源、賽事數量與警告。
6. 更新 `index.html` 的資源版本，避免瀏覽器讀到舊快取。

不要手動維護 `data/public-data.js` 中的大量資料；應修改試算表或 CSV，再執行更新工具。

## 4. 核心資料模型

- `records`：逐場賽果、日期、賽事、正反方、比分、勝方、時段與會場。
- `honors`：團體或個人公開榮譽。
- `entities`：學校、特殊隊伍、大學的固定代碼、正式名稱與別名。
- `attendance`：選手登場紀錄，目前保留但尚未在前台呈現。
- `topics`：賽事辯題與說明。
- `generatedAt`：資料檔產生時間；首頁「上次修改」以此為唯一依據。
- `siteContent`：由 `data/site-content.csv` 產生，控制 Banner 下方的英文小標、中文標題與兩段網站介紹。

賽事數量不是固定設定值。`app.js` 會合併 `records`、`honors`、`topics` 中的賽事名稱後去重計算，首頁狀態列與統計區都使用這個結果。

## 5. 畫面責任

- `index.html`：語意結構、SEO metadata、結構化資料與畫面容器。
- `styles.css`：全站視覺、桌機／手機響應式版面。
- `app.js`：首頁統計、時間軸、賽事卡、排行榜、賽事詳情、辯論總覽及搜尋結果。
- `js/interactions.js`：導覽、按鈕、篩選與使用者操作。
- `js/personal-records.js`：使用者個人成績、平均分數與 CSV 備份；資料只存在該瀏覽器的 `localStorage`，不可當成公開資料或後台資料。
- `js/core.js`：不可見的共用資料處理層。
- `js/site-config.js`：可替換的外部連結設定。

「辯論總覽」依序分成賽事、學校與辯題三個分頁。賽事可依名稱、辯題與年份篩選；學校清單只列 `entities` 中類型為 `s` 的正式學校，點擊後沿用學校完整紀錄元件，顯示參加賽事、場次、勝場與相關榮譽；辯題分頁直接呈現 `topics`，可依辯題、說明或賽事名稱篩選。全站搜尋也會比對辯題，辯題卡以 `competitionName` 開啟總覽內的所屬賽事詳情，不另建重複資料來源。

「建檔」是個人用的測試功能，與公開資料管線完全分離。使用者可逐場輸入盃賽、姓名、申論、質詢、答辯、結辯與勝負，或匯入本功能輸出的 CSV；資料暫存在瀏覽器，清除網站資料或更換裝置後可能消失，因此介面必須持續顯示下載 CSV 備份的提醒。

## 6. SEO 與 AI 搜尋入口

- `index.html`：標題、描述、canonical、Open Graph、robots 指示與 JSON-LD。
- `robots.txt`：允許一般搜尋引擎擷取並指向 sitemap。
- `sitemap.xml`：正式首頁索引。
- `llms.txt`：向 AI 系統說明網站定位、內容範圍、重要入口與引用邊界。
- `debate-records.html`：可獨立索引的辯論戰績與辯論積分說明頁；資料總數由更新程式自動同步。
- 首頁的「台灣高中辯論賽事與公開戰績資料庫」段落：提供不依賴 JavaScript 的主題語意。

每次執行更新工具，`sitemap.xml` 的 `lastmod` 與 `debate-records.html` 的資料總數都會同步更新。

不要以隱藏字詞或重複堆疊關鍵字做 SEO。新增內容時應使用自然、可見、可驗證的文字，並維持「辯論比賽、高中辯論、高中職辯論、辯論戰績、辯論積分、辯題」等核心主題的一致性。

## 7. 安全修改規則

1. 先讀現有資料與呈現方式，不要自行發明另一套欄位或覆蓋既有 CSV。
2. 新增賽事應新增一個 `public-data-賽事名稱.csv`；更新工具採合併、去重，不應刪除歷史資料。
3. 單位名稱與別名應透過固定代碼歸戶，避免同校被拆成多筆排行。
4. 更動載入順序、資料模型或手機版面後，必須在瀏覽器實測首頁、總覽三個分頁、搜尋、建檔與資料回報。
5. 資料更新後至少檢查 `data/update-report.txt` 的賽事數、筆數與警告。
6. 發布時應一併提交來源資料、產生資料、名冊、程式、SEO 檔案及帶新版本號的 `index.html`。

## 8. 最小驗證清單

- JavaScript 語法檢查無錯。
- 首頁顯示的修改日期等於 `generatedAt` 的月日。
- 首頁狀態列與「已收錄賽事」統計數字一致。
- 首頁時間軸、賽事卡與三個排行榜均有內容。
- 賽事可依名稱／辯題／年份搜尋並開啟詳情。
- 建檔可新增下一場、計算平均申論分，重新整理後仍可讀取，且 CSV 可匯出再匯入。
- 學校／選手搜尋可用，資料回報按鈕指向設定表單。
- 手機沒有明顯橫向溢出，狀態列可讀。
- `robots.txt`、`sitemap.xml`、`llms.txt` 可由網站根目錄存取。
